/** Categories for the input error-icon sub-theme (layout only — scales with the input via CSS) */
export const INPUT_ERROR_ICON_CATEGORIES = ['display', 'items', 'justify', 'position', 'pointerEvents', 'hide'] as const;
