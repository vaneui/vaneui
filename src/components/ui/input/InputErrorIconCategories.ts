/** Categories for the input error-icon sub-theme (size drives the icon's own --fs/--px scale) */
export const INPUT_ERROR_ICON_CATEGORIES = ['size', 'display', 'items', 'justify', 'position', 'pointerEvents', 'hide'] as const;
