import type { InputWrapperProps } from "./InputWrapperProps";

/** Default props for the input wrapper element */
export const inputWrapperDefaults: Partial<InputWrapperProps> = {
  relative: true,
  flex: true,
  wFull: true,
};
