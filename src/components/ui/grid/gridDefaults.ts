import type { GridProps } from "./GridProps";

/** Default props shared by all Grid components (Grid2-Grid6) */
export const gridDefaults: Partial<GridProps> = {
  md: true,
  grid: true,
  gap: true,
  noPadding: true,
  outline: true,
  sharp: true,
};
