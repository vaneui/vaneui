import { defaultTextTheme } from "../typography/text/defaultTextTheme";
import { fieldDescriptionDefaults } from "./fieldDescriptionDefaults";

/** Field's help text: the Text theme, muted. */
export const defaultFieldDescriptionTheme = defaultTextTheme.withDefaults(fieldDescriptionDefaults);
