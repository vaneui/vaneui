import { defaultColTheme } from "../col/defaultColTheme";
import { fieldControlRowDefaults } from "./fieldControlRowDefaults";

/** Field's inline control row: the Col theme turned into a centred row. */
export const defaultFieldControlRowTheme = defaultColTheme.withDefaults(fieldControlRowDefaults);
