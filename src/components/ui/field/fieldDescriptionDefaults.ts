import type { TypographyProps } from "../typography/common";

/** Field's help text: muted, one step down from the control. */
export const fieldDescriptionDefaults: Partial<TypographyProps> = {
  sm: true,
  secondary: true,
  fontSans: true,
  textLeft: true,
  outline: true,
  noMargin: true,
};
