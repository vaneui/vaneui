import { LAYOUT_FULL } from "../props/categoryBuilders";

/** The inline row holds a control and its label side by side; layout only, no surface of its own. */
export const FIELD_CONTROL_ROW_CATEGORIES = [
  ...LAYOUT_FULL,
] as const;
