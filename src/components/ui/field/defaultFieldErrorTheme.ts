import { defaultTextTheme } from "../typography/text/defaultTextTheme";
import { fieldErrorDefaults } from "./fieldErrorDefaults";

/** Field's error text: the Text theme, danger. */
export const defaultFieldErrorTheme = defaultTextTheme.withDefaults(fieldErrorDefaults);
