/** Categories for the select chevron sub-theme (size drives the chevron's own --fs/--px scale) */
export const SELECT_CHEVRON_CATEGORIES = ['size', 'display', 'items', 'justify', 'position', 'pointerEvents', 'hide'] as const;
