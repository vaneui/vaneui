import type { SelectWrapperProps } from "./SelectWrapperProps";

/** Default props for the select wrapper element */
export const selectWrapperDefaults: Partial<SelectWrapperProps> = {
  relative: true,
  flex: true,
  wFull: true,
};
