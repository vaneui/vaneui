/** Categories for button spinner sub-theme (layout only, no appearance/size — inherits via CSS variables) */
export const BUTTON_SPINNER_CATEGORIES = ['position', 'display', 'items', 'justify', 'overflow', 'hide'] as const;
