import type { CaptionProps } from "./CaptionProps";

/** Default props for the Caption component */
export const captionDefaults: Partial<CaptionProps> = {
  md: true,
  secondary: true,
  textLeft: true,
};
