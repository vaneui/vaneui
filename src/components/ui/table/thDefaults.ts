import type { ThProps } from "./ThProps";

/** Default props for the Th (header cell) component */
export const thDefaults: Partial<ThProps> = {
  md: true,
  secondary: true,
  outline: true,
  borderB: true,
  padding: true,
  semibold: true,
  textLeft: true,
};
