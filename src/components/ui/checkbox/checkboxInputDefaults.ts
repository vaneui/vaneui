import type { CheckboxProps } from "./CheckboxProps";

/** Default props for Checkbox input component */
export const checkboxInputDefaults: Partial<CheckboxProps> = {
  md: true,
  primary: true,
  border: true,
  rounded: true,
  noRing: true,
  noShadow: true,
  filled: true,
  focusVisible: true,
  cursorPointer: true,
};
