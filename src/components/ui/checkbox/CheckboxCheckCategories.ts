/** Categories for checkbox check mark sub-theme (layout only, no appearance/size — inherits via CSS variables) */
export const CHECKBOX_CHECK_CATEGORIES = ['display', 'items', 'justify', 'position', 'hide'] as const;
