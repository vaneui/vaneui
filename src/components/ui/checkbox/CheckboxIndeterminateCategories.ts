/** Categories for checkbox indeterminate mark sub-theme (layout only, no appearance/size — inherits via CSS variables) */
export const CHECKBOX_INDETERMINATE_CATEGORIES = ['display', 'items', 'justify', 'position', 'hide'] as const;
