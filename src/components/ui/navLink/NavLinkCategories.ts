import { UI_ELEMENT_CATEGORIES } from "../props/categoryBuilders";

/** NavLink uses the same categories as other interactive UI elements (Button, Badge, etc.) */
export const NAV_LINK_CATEGORIES = UI_ELEMENT_CATEGORIES;
