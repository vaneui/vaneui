import type { ModalBodyProps } from "./ModalBodyProps";

/** Default props for ModalBody component */
export const modalBodyDefaults: Partial<ModalBodyProps> = {
  flex: true,
  column: true,
  gap: true,
  overflowAuto: true,
  padding: true,
};
