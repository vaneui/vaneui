import type { OverlayProps } from "../overlay";

/** Default props for Modal overlay */
export const modalOverlayDefaults: Partial<OverlayProps> = {
  fixed: true,
  flex: true,
  itemsCenter: true,
  justifyCenter: true,
};
