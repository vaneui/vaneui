import type { ModalFooterProps } from "./ModalFooterProps";

/** Default props for ModalFooter component */
export const modalFooterDefaults: Partial<ModalFooterProps> = {
  flex: true,
  row: true,
  itemsCenter: true,
  justifyEnd: true,
  gap: true,
  padding: true,
};
