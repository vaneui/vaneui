import type { CardBodyProps } from "./CardBodyProps";

/** Default props for CardBody component */
export const cardBodyDefaults: Partial<CardBodyProps> = {
  flex: true,
  column: true,
  gap: true,
  overflowAuto: true,
  padding: true,
};
