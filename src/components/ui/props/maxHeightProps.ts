/**
 * Max-height props for components.
 * When enabled, applies a size-dependent maximum height via --max-height CSS variable.
 */

export interface MaxHeightProps {
  /** Apply size-dependent maximum height (uses --max-height CSS variable) */
  maxHeight?: boolean;
}
