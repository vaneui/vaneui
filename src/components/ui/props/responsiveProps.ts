/**
 * Responsive props for automatic breakpoint-based CSS variable switching
 */

export interface ResponsiveProps {
  /** Enable responsive CSS variable switching for automatic size adaptation across breakpoints */
  responsive?: boolean;
}
