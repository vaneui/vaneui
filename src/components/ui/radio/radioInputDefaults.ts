import type { RadioProps } from "./RadioProps";

/** Default props for Radio input component */
export const radioInputDefaults: Partial<RadioProps> = {
  md: true,
  primary: true,
  border: true,
  pill: true,
  noInsetRing: true,
  noShadow: true,
  filled: true,
  focusVisible: true,
  cursorPointer: true,
};
