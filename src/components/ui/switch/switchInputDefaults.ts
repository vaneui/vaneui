import type { SwitchProps } from "./SwitchProps";

/** Default props for Switch input component (the track) */
export const switchInputDefaults: Partial<SwitchProps> = {
  md: true,
  primary: true,
  border: true,
  pill: true,
  noInsetRing: true,
  noShadow: true,
  filled: true,
  focusVisible: true,
  cursorPointer: true,
  transition: true,
};
