/** Categories for the switch thumb sub-theme (no appearance/size — it inherits both via CSS variables) */
export const SWITCH_THUMB_CATEGORIES = [
  'display', 'items', 'justify', 'justifySelf', 'position', 'hide', 'shape', 'transition', 'pointerEvents',
] as const;
