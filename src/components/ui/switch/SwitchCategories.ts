import { CHECKBOX_CATEGORIES } from "../checkbox/CheckboxCategories";

/** Switch reuses Checkbox's category set — same boolean control surface (incl. `alignSelf` for Label alignment). */
export const SWITCH_CATEGORIES = CHECKBOX_CATEGORIES;
