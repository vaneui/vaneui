import { defaultPopupTheme } from "../popup/defaultPopupTheme";
import { tooltipDefaults } from "./tooltipDefaults";

/** Tooltip: the popup theme with label-sized, filled defaults. */
export const defaultTooltipTheme = defaultPopupTheme.withDefaults(tooltipDefaults);
