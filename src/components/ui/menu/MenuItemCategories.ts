import { UI_ELEMENT_CATEGORIES } from "../props/categoryBuilders";

/** MenuItem uses the same categories as other interactive UI elements (Button, Badge, etc.) */
export const MENU_ITEM_CATEGORIES = UI_ELEMENT_CATEGORIES;
