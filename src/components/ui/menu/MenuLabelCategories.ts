import { UI_ELEMENT_CATEGORIES } from "../props/categoryBuilders";

/** MenuLabel uses the same categories as other interactive UI elements */
export const MENU_LABEL_CATEGORIES = UI_ELEMENT_CATEGORIES;
