import type { MenuGroupProps } from "./MenuGroupProps";

/** Default props for MenuGroup component */
export const menuGroupDefaults: Partial<MenuGroupProps> = {
  flex: true,
  column: true,
  noGap: true,
  noPadding: true,
};
