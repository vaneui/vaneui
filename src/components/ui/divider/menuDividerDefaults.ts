import type { DividerProps } from "./DividerProps";

/** Default props for MenuDivider (compact divider used inside menus) */
export const menuDividerDefaults: Partial<DividerProps> = {
  sm: true,
  outline: true,
  inherit: true,
  paddingY: true,
  horizontal: true,
};
